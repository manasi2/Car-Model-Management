import { ValidateAlphanumericDirective } from './validate-alphanumeric.directive';

describe('ValidateAlphanumericDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateAlphanumericDirective();
    expect(directive).toBeTruthy();
  });
});
